# 产品决策记录

## 取消预约码

使用机器人位置与用户位置的实时距离完成自动汇合。阈值为 10 米，避免额外输入步骤与现场沟通成本。

## 安全事件优先级

- `LOCK` 是全局顶级安全事件，不依赖电子围栏。
- `ENTER` 在机器人进入围栏时立即触发。
- `DWELL` 在机器人停留超过阈值时触发，但 `POINT` 状态豁免。
- `OFFLINE` 与 `NONE` 不参与围栏判定。

## 数据边界

公开项目仅包含经过重构与脱敏的作品集内容。所有运营数字均为 Mock 演示数据，不代表真实生产成果。
